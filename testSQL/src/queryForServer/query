CREATE TABLE users ( id INT(11) NOT NULL AUTO_INCREMENT PRIMARY KEY, name VARCHAR(100) NOT NULL, pwd VARCHAR(255) NOT NULL, email VARCHAR(100) NOT NULL, username VARCHAR(7) NOT null, created_at DATETIME NOT NULL DEFAULT CURRENT_TIME, UNIQUE(email, username) );
